/**
 * DE: Initialisiert die Summary für spätere Dashboard-Daten.
 * EN: Initializes the summary for future dashboard data.
 */
function initializeSummary() {
  getTasks();
}


document.addEventListener("DOMContentLoaded", initializeSummary);
