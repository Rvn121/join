/**
 * DE: Initialisiert die Kontakte für die weitere Umsetzung.
 * EN: Initializes contacts for further implementation.
 */
function initializeContacts() {
  getContacts();
}


document.addEventListener("DOMContentLoaded", initializeContacts);
