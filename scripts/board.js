/**
 * DE: Initialisiert das Board für die weitere Umsetzung.
 * EN: Initializes the board for further implementation.
 */
function initializeBoard() {
  getTasks();
}


document.addEventListener("DOMContentLoaded", initializeBoard);
